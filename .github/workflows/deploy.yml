name: hugo deploy
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@main
        with:
              submodule: true 
      - name: Hugo-Site
        uses: chabad360/hugo-actions@v2

 
